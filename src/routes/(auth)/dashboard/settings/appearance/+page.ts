// src/routes/(auth)/dashboard/settings/appearance/+page.ts
//
// Єдиний вміст сторінки — вибір теми, а він живе в localStorage браузера.
// На сервері цього значення немає взагалі: mode-watcher підставляє там
// дефолт 'system'. Тому при прямому заході (або F5) сервер малював
// «Системна» як обрану, і через сотні мілісекунд, коли доїжджала
// гідратація, підсвітка стрибала на справжній варіант.
//
// Показувати неправильний стан гірше, ніж показувати його на мить пізніше:
// рейка розділів приходить із SSR як була, а картки теми з'являються вже
// одразу з правильною.
//
// Це стосується САМЕ цієї сторінки. Решта налаштувань має серверні дані
// й SSR їм потрібен — не копіюй цей файл у сусідні розділи.
export const ssr = false
