// Кореневий лейаут виконується на КОЖНІЙ навігації — і на публічних
// сторінках, і на всьому /dashboard. Тому тут не лишилось жодного запиту
// до бази: віддаємо тільки сесію, яку hooks.server.ts уже резолвнув.
//
// Лічильник сповіщень раніше рахувався саме тут. Він переїхав у стор
// сповіщень, який тягне його один раз за сесію (/api/me/badges) і далі
// оновлює подіями Pusher. Причина — тариф Neon: він рахує ЧАС РОБОТИ
// бази, тож запит на кожну навігацію тримав її розбудженою даремно.

import type { LayoutServerLoad } from './$types'

export const load: LayoutServerLoad = async ({ locals }) => {
  return { session: locals.session }
}
