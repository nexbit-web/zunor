<script lang="ts">
  // Заглушка іконки для компонентних тестів.
  //
  // Замінює собою всю бочку lucide-svelte (див. плагін у vitest.config.ts).
  // Іконка не має поведінки: у тестах вона потрібна лише як «тут щось
  // намальовано». Тому перевіряти «яка саме іконка» компонентні тести НЕ
  // можуть — і не мусять: підпис поруч однаково важливіший.

  let props: Record<string, unknown> = $props()
</script>

<svg data-icon="stub" aria-hidden="true" {...props}></svg>
