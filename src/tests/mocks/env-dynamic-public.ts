// Заглушка для $env/dynamic/public. Див. пояснення в env-static-private.ts.

export const env: Record<string, string | undefined> = {
  PUBLIC_PUSHER_KEY: 'test-pusher-key',
  PUBLIC_PUSHER_CLUSTER: 'eu',
}
